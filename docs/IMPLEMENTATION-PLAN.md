# AGAIN rebuild plan
1. Establish typed, versioned workflow model, validation, draft journal and transaction-safe SQLite storage.
2. Implement common execution pipeline with variables, predicates, retry, per-item isolation, pause/cancel and verified output publication.
3. Implement proportional image operations and file tools; test varying orientations and crop/text persistence.
4. Implement Windows recorder and guarded semantic replay, browser adapter, Photoshop integration and honest app profiles.
5. Implement desktop UI with recovery, editing, preview, workflow library, settings and run history.
6. Build self-contained x64 output and installer; run controlled Windows and browser acceptance tests.
7. Compare every numbered requirement with evidence. Do not equate source presence with verified completion.

Release gate: all requested acceptance tests A–I must pass. External-application results require the actual applications. A Linux cross-build is not evidence that Windows launch or installation passed.
