# Contributing
Build and test before submitting changes. Keep application connectors optional. Add regression tests for behavioral changes, particularly crop/text persistence, safe outputs and recovery. Do not commit private inputs, workflow databases, credentials or signing keys. Never claim an external application passed unless it was installed and used. Keep the requirement matrix honest.
